# Core model runs on the Python standard library only (math, dataclasses).
# These are for the next steps (plots, sweeps, accurate fluid properties):
numpy
matplotlib
CoolProp        #  temperature-dependent fluid properties
